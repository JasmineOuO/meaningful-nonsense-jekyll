name: Meaningless Nonsense
title: Meaningful Nonsense
url: "https://jasmineouo.github.io/Blog"
github_username:  JasmineOuO
repository: JasmineOuO/Blog
email: jasmineou99@gmail.com
description: >-
  This is a blog built from scratch to learn the basics of frontend 
  development and to give my sister a place to improve her writing 
  with blogging. The blog is generated with Jekyll through the use 
  of Liquid-enhanced HTML templates and hosted on Github Pages.

markdown: kramdown
kramdown:
  input: GFM # Enable GitHub Flavored Markdown (fenced code blocks)
  hard_wrap: false
highlighter: rouge

permalink: /:title
      
defaults:
  - scope:
      path: "resources/images/polaroids"
    values:
      image: true

plugins:
  - jekyll-feed
  #- searchyll

exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - .vs/slnx.sqlite
  - node_modules

staticman:
  branch: "master"
  
#elasticsearch:
  #index_name: blog

  # Indexing Blog Posts in Elasticsearch
  # uncomment {- searchyll, elasticsearch: , index_name: blog} lines from above and {gem "searchyll"} from Gemfile
  # bundle install
  # BONSAI_URL=<YOUR KEY HERE> bundle exec jekyll serve
  # re-comment the lines
  # bundle install
  # bundle exec jekyll build
  # bundle exec jekyll serve
  # BONSAI_URL=blah bundle exec jekyll serve
  # verify that it doesn't index anymore
